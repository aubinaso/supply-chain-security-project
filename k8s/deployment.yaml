# Déploiement de l'app — À DÉPLOYER PAR DIGEST (pas par tag).
# ➜ Remplacez l'image ci-dessous par VOTRE digest signé (sortie de `echo $DIGEST` au Lab 0/2).
#    ex : ghcr.io/<votre-user>/scs-demo-app@sha256:abcd1234...
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scs-demo-app
  namespace: app
  labels: { app: scs-demo-app }
spec:
  replicas: 2
  selector:
    matchLabels: { app: scs-demo-app }
  template:
    metadata:
      labels: { app: scs-demo-app }
    spec:
      containers:
        - name: app
          image: ghcr.io/johlan78/scs-demo-app@sha256:6274762da84c8f74925c2ebf676c832ad98252b880376c75ba27de5384b90721
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits: { cpu: "250m", memory: "128Mi" }
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
---
apiVersion: v1
kind: Service
metadata:
  name: scs-demo-app
  namespace: app
spec:
  type: NodePort
  selector: { app: scs-demo-app }
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080     # mappé sur localhost:8080 par kind-config.yaml
